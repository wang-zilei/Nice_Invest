# 项目一阶段复盘 — Nice Invest 金融分析 Multi-Agent 系统

> 时间跨度：2026-05-15 ~ 2026-05-19（5天，11个阶段）
> 复盘范围：项目启动 → GitHub 开源 + Sealos 国内云部署
> 一句话定位：从零独立交付了一个 LangGraph + ReAct 金融分析 Multi-Agent 全栈系统

---

## 目录

1. [Agent 框架搭建](#1-agent-框架搭建)
2. [UI/UX 设计](#2-uiux-设计)
3. [网站部署](#3-网站部署)

---

# 1. Agent 框架搭建

## 1.1 系统架构总览

### 整体架构

```
用户输入（股票代码/名称）
    │
    ▼
┌─────────────────────────────────────┐
│            Router（路由判断）          │
│  - 股票代码格式标准化                  │
│  - 确定分析范围（全部分析/单维度）       │
└──────────────┬──────────────────────┘
               │ LangGraph Send（并行分发）
    ┌──────────┼──────────┬──────────┐
    ▼          ▼          ▼          ▼
┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐
│基本面 │  │技术面 │  │ 估值  │  │新闻舆情│
│Agent │  │Agent │  │Agent │  │Agent │
│ReAct │  │ReAct │  │ReAct │  │ReAct │
└──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘
   │         │         │         │
   └─────────┼─────────┼─────────┘
             │ add reducer（并行写入 state）
             ▼
┌─────────────────────────────────────┐
│          Summary Agent（ReAct）       │
│  cross_validate + verify_consistency │
│  + calculate_weighted_score          │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│       结构化投资报告（SSE 推送前端）     │
│  五段式：投资倾向→交叉分析→综合评分      │
│  →情景分析→风险清单                    │
└─────────────────────────────────────┘
```

### 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 编排 | LangGraph StateGraph + Send API | 状态机驱动，4 Agent 并行分发 |
| Agent | LangChain `bind_tools` + ReAct 循环 | Thought→Action→Observation 推理 |
| LLM | ChatOpenAI（兼容协议） | 支持 DeepSeek/GPT-4o/Qwen 及任意兼容端点 |
| 数据 | akshare → Tushare Pro → yfinance | 三级降级 |
| 计算 | 自定义 Python 函数 | 杜邦分析/PEG/CAGR/财务比率 |
| API | FastAPI + SSE | 流式推送分析进度 |
| 前端 | React 19 + TypeScript | 3 页 SPA |

## 1.2 LangGraph 多 Agent 协作机制

项目的编排核心在 [graph.py](https://github.com/wang-zilei/Nice_Invest/blob/master/src/graph.py?file=src/graph.py) 的 `build_graph()` 函数中，关键设计点：

**并行分发（Send API）**

LangGraph 默认的 `add_conditional_edges` 只能路由到单节点。实现并行需要 `Send` API：

```python
# graph.py:491 — 路由函数返回 Send 列表
def _route_agents(state):
    agents = ["fundamental", "technical", "valuation", "news"]
    return [Send(agent, state) for agent in agents]
```

LangGraph 检测到返回 `Send` 列表后，会并行执行所有目标节点。4 个 Agent 互不依赖，各自独立完成 ReAct 循环。

**结果合并（add Reducer）**

并行 Agent 需要写入同一个 `agent_results` 列表，LangGraph 的 `Annotated` 类型支持自定义 reducer：

```python
# state.py
agent_results: Annotated[list, add]  # add reducer 实现并行写入
```

每个 Agent 执行完后写入自己的结果，LangGraph 自动将并行分支的 return 合并为一个列表。这个设计避免了引入额外的消息队列或锁。

**ReAct 循环实现**

每个 Agent 的实际执行在 `run_agent_react()`（graph.py:163）中：

1. LLM 收到分析指令 + 工具列表
2. LLM 返回 tool_call → 代码查找对应工具并 invoke
3. ToolMessage 追加到对话历史 → LLM 再次判断
4. 直到 LLM 不再返回 tool_call（认为分析完成）或达到 max_steps=12
5. 整个循环包裹在线程池 `ThreadPoolExecutor` 中，`AGENT_TIMEOUT=180s` 整体兜底

每轮 tool_call 的查找逻辑根据 agent_type 动态选择工具集（FUNDAMENTAL_TOOLS / TECHNICAL_TOOLS 等），工具调用链路完全可追溯。

**Summary Agent 的交叉验证**

Summary Agent 拥有 3 个专属工具（summary.py）：

- `cross_validate_agents`：两两对比不同 Agent 的核心结论，检测矛盾
- `verify_data_consistency`：同一指标在不同数据源的值是否一致（偏差>10% → 标记显著偏差）
- `calculate_weighted_score`：按权重计算综合评分（基本面35% / 技术面20% / 估值30% / 新闻15%）

这三个工具的存在使 Summary 不是简单的文本拼接，而是有分析推理能力的 Agent。

## 1.3 各 Agent 的 Prompt 设计

### 输出铁律体系（template.py）

所有 Agent 共享 **7 条铁律**：

| 铁律 | 内容 | 必要性 |
|------|------|--------|
| ① 禁止暴露思考过程 | 不得出现"好的"、"现在我来"等过渡语 | DeepSeek 模型天然倾向输出推理链，不约束就会泄漏到用户看到的结果里 |
| ①b 禁止 Markdown 符号 | 不得使用 `#`/`**`/`---` 等 | 前端用中文编号（一/二/三）和仿宋字体做层级，Markdown 符号会和渲染逻辑冲突 |
| ② 数据来源标注 | 每个数据点标注 `[来源: XXX]` | 面试场景核心要求：可解释、可追溯 |
| ③ 失败兜底声明 | 数据失败时如实声明，不捏造 | 防幻觉第一道防线 |
| ④ 量化优于定性 | 用数字不用模糊形容词 | 金融分析的专业性要求 |
| ⑤ 不确定性标注 | LLM 知识库补充的内容标注 `[注: 基于估算]` | 区分实时数据和模型知识 |
| ⑥ 完整输出 | 每维度 ≥ 200 字 | 防止模型偷懒只写摘要 |

**教训**：铁律 ① 和 ①b 是在实际使用后才加的。第一版 prompt 没这么强硬，结果 DeepSeek 输出大量"好的，数据已获取，现在我来分析……"，用户体验很差。后面才加了服务端清洗函数 `_clean_agent_output()`，用正则匹配 12 种过渡句式逐行删除。

### 两段式输出设计

```
纯文本正文（人读，用中文编号分层）
  ↓
```json
{...结构化数据...}
```
```

前端从 Markdown 中提取 JSON 代码块渲染指标卡片和评分图表；解析失败时降级为纯 Markdown 展示。这个设计让 Agent 的输出同时服务于"人看"和"机读"两种场景。

**教训**：JSON Schema 中的所有花括号都需要转义为 `{{` `}}`，因为 `ChatPromptTemplate.from_messages()` 内部使用 Python f-string 格式解析。第一版没有转义导致所有 Agent 初始化崩溃（Bug B1），是一个容易被忽略的工程细节。

### 各 Agent 的专属维度

| Agent | 分析维度 | 核心数据工具 |
|-------|---------|-------------|
| 基本面 | 盈利能力/成长性/偿债能力/运营效率 | 财务摘要 + 杜邦 + CAGR |
| 技术面 | 趋势判断/量价关系/支撑压力位/波动率 | 60日OHLCV + MA5/10/20/60 |
| 估值 | PE/PB/PS/PEG/股息率/行业对比 | 估值快照 + PEG计算 |
| 新闻 | 情感分析/重大事件/资金面 | 东方财富个股新闻 + 财联社快讯 |
| Summary | 交叉验证/数据一致性/综合评分 | cross_validate + verify_consistency + weighted_score |

### 评判 Agent（独立调用，不在前端展示）

评判 Agent 设计为内部工具，4 维度打分：

1. 幻觉检测 — 输出事实是否与原始数据一致
2. 推理质量 — 逻辑完整性、结论是否有数据支撑
3. 风险敏感度 — 是否识别到关键风险
4. 工具调用准确率 — 工具选择、参数、调用链是否合理

**现状**：评判 Agent 的代码已写（graph.py:325 的 `evaluation_node`），但项目重心放在了搭建 Agent 和部署上线，**尚未进行真实的评测工作**。这是下一阶段的核心任务。

## 1.4 MCP 工具链与 Function Call 的实现方式

### 工具注册机制（以基本面 Agent 为例）

```python
# analyst.py
@tool
def get_fundamental_backup(ts_code: str) -> str:
    """获取基本面数据备选（akshare），当 Tushare 限流时使用"""
    return get_stock_financial_summary(ts_code)

FUNDAMENTAL_TOOLS = [
    get_fundamental_backup,      # akshare（主力）
    get_fundamental_data,        # Tushare Pro（备选1）
    get_fundamental_data_yahoo,  # yfinance（海外兜底）
    calculate_dupont_analysis,   # Function Call 计算工具
    calculate_growth_rate,       # Function Call 计算工具
]
```

每个工具用 `@tool` 装饰器声明，LangChain 自动从函数签名生成 OpenAI Function Calling schema。工具按优先级排列：akshare → Tushare → yfinance。LLM 看到工具列表后自行判断调用哪个——描述中的"当 Tushare 限流时使用"和"兜底"等提示词引导 LLM 按优先级选择。

### 工具调用链路

```
LLM 输出 tool_calls: [{name: "get_fundamental_backup", args: {ts_code: "000001.SZ"}}]
  → graph.py:232 根据 agent_type 查找工具注册表
  → tool_obj.invoke(tool_args)
  → news_api.py: get_stock_financial_summary() → akshare HTTP 调用（15s 超时）
  → 结果作为 ToolMessage 追加到对话历史
  → LLM 再次判断：数据够了吗？→ 够 → 输出分析报告
                         → 不够 → 再调用下一个工具
```

### 为什么没有使用标准 MCP 协议？面试时怎么解释？

#### 技术事实（必须分清，不能混淆）

项目实际使用的是 **LangChain `@tool` 装饰器 + `bind_tools()` + OpenAI Function Calling 协议**。

**Function Calling ≠ MCP，这是两个不同层面的东西**：

- **Function Calling**：LLM 的一种**能力**——模型能返回符合指定 JSON Schema 的响应，从而"调用"一个函数。OpenAI、DeepSeek、Qwen 等主流模型都支持。
- **MCP（Model Context Protocol）**：Anthropic 推出的一个**通信协议**——定义了 Client-Server 架构、工具发现机制、传输通道（stdio/SSE）。它的作用是让不同应用能共享同一套工具。

两者的关系是：MCP 定义了"工具怎么被 LLM 发现和调用"的协议标准；Function Calling 是"LLM 收到工具定义后怎么返回调用指令"的模型能力。**MCP 可以用 Function Calling 作为底层，但 Function Calling 不等于 MCP。**

| 维度 | 标准 MCP | 本项目的实现 |
|------|---------|------------|
| 本质 | 通信协议（Client-Server + JSON-RPC） | LLM 能力（模型返回 tool_call JSON） |
| 进程模型 | 独立 MCP Server 进程 | 同进程内 Python 函数直接调用 |
| 工具发现 | MCP Client 通过协议拉取 tool list | LangChain `@tool` 装饰器 → `bind_tools()` 注入 |
| 传输通道 | stdio / SSE | 无（进程内调用） |
| 适用场景 | 多应用共享工具、跨模型复用 | 单一应用、快速迭代 |

#### 危险做法：在简历和面试中滥用"MCP"标签

**如果你用了 LangChain 本地 `@tool`，却在简历上写"基于 MCP 协议构建工具链"，面试官一旦追问细节，你会当场翻车。**

典型翻车场景：
> 面试官："你的 MCP Server 是独立部署的还是挂在同一个进程？用的什么传输通道，stdio 还是 SSE？"
> 你："呃……"
> 面试官心理：概念不清 or 技术造假 → 直接 pass

MCP 这个词在技术面试官那里有非常具体的含义（独立进程、JSON-RPC、Client-Server 架构）。不要赌面试官不懂技术。

#### 面试中的正确表述：三层递进

---

**第一层：PM / 非技术面试官（大概率停在这一层）**

> "我设计了一套基于 Function Calling 的标准化工具链，在架构设计上参考了 MCP 工具与 Agent 解耦的规范——每个数据源（akshare/Tushare/yfinance）和计算函数（杜邦/PEG）都封装为独立的 tool，有标准化的输入输出 JSON Schema，LLM 通过 function call 机制自动发现和调用。工具调用链路从 LLM 决策 → 工具选择 → 参数传递 → 结果返回，全程可追溯。"

**关键**：不要说"我实现了 MCP"，说"我参考了 MCP 的架构思想，用 Function Calling 实现了一套标准化工具链"。这既拔高了立意（你知道 MCP 是什么、它的设计规范是什么），又符合事实（你实现的是 Function Calling 工具链）。面试官会觉得你不仅知其然，还知其所以然。

---

**第二层：技术面试官追问实现细节（展示工程判断力）**

> "具体实现上，我选择了 LangChain 的 tool binding 机制 + OpenAI Function Calling 协议，而不是标准 MCP 的独立进程架构。这是基于项目约束做的技术折中——
> 
> 这个项目是 5 天从零交付的 MVP，目标是大厂面试展示核心链路（Agent 编排 + 评测）。标准 MCP 需要额外搭建 MCP Server 进程、管理生命周期、处理 JSON-RPC 序列化，在这个阶段引入独立进程通信并不增加核心价值，反而显著增加调试复杂度。Function Calling 是更成熟、开箱即用的方案——改完工具定义立刻生效，不需要重启 MCP Server、重新握手连接。
> 
> 但我的工具设计（name + description + JSON Schema parameters）和 MCP 的 tool schema 在结构上是兼容的，如果后续需要跨项目复用这些金融数据工具，把它们封装为标准 MCP Server 是自然的一步，Agent 侧的代码完全不需要改。"

**为什么这是加分项**：你展示了——
- 知道 MCP 是什么、怎么运作的
- 知道 Function Calling 是什么、两者区别在哪
- 在当前约束下做了有意识的架构选择，而不是"不会用"
- 为未来升级留好了路径

这不是"技术不足"，这是"技术折中（Trade-off）能力"——大厂面试官想要的就是这种判断力。

---

**第三层：资深工程师深挖（展示架构前瞻性）**

> "更深一层看，不管是 OpenAI 的 Function Calling 还是 MCP 的 tools/call，核心都是向 LLM 传递同一份 JSON Schema 格式的工具定义。LangChain 已经支持 MCP Adapter（`langchain_mcp_adapters`），可以把标准 MCP Server 的工具直接加载为 LangChain tool。我现在的 `@tool` 声明和标准 MCP 的 tool schema 在结构上是等价的，迁移成本极低——只需把工具函数包装为 MCP Server 的 tool handler，Agent 侧的 `bind_tools()` 调用逻辑完全不变。"

**为什么这是加分项**：你证明了当前的 MVP 实现在架构上已经为未来的微服务化和跨生态接入留好了扩展性。面试官会认为你是在"做正确的事，用正确的方式做"，而不是"先写着再说"。

---

#### 简历 Bullet 的修正建议

当前简历 bullet 2 建议修正为：

> **修正前**（有风险）："MCP工具链与Function Call集成"
>
> **修正后**（安全且出彩）："设计标准化 Function Calling 工具链（参考 MCP 规范），集成三级数据源与财务计算函数，工具调用链路可追溯"

这样写的好处：
- "Function Calling 工具链" → 技术事实准确
- "参考 MCP 规范" → 展示行业视野，你知道 MCP 是什么
- 不提"MCP 实现" → 不会被追问问翻车
- "工具调用链路可追溯" → 产品经理视角的核心价值

---

#### 总结：这一节在面试中的使用指南

| 面试官类型 | 怎么说 | 风险等级 |
|-----------|--------|---------|
| PM/HR 面 | 用第一层表述，"参考 MCP 规范设计的标准化工具链" | 安全 |
| 技术面（一般追问） | 用第二层表述，展示技术折中判断 | 安全，加分 |
| 技术面（深挖） | 用第三层表述，展示架构前瞻性 | 安全，加分 |
| **任何情况都不要** | "我实现了 MCP 工具链" | **翻车** |

### 数据源三级降级策略

```
akshare（东方财富/同花顺，国内免费不限流）
  ↓ 超时/失败
Tushare Pro（API Token 认证，有频率限制 1次/分钟）
  ↓ 超时/限流/无 Token
yfinance（Yahoo Finance，海外可用，国内延迟较高）
```

这个三级策略不是一开始就有的。最初所有 Agent 只用 Tushare，后来遇到 Tushare 频率限制（`stock_basic` 1次/分钟），加了 akshare 作为备选；再后来 Render 美国服务器部署后发现东方财富等国内源被墙，又加了 yfinance 作为海外兜底。切到 Sealos 国内云后，优先级翻回 akshare 主力。

**核心教训**：数据源优先级不是绝对的，取决于部署环境。国内部署 → akshare 优先；海外部署 → yfinance 优先。

## 1.5 Agent 框架的迭代过程

### 迭代历程

```
v0（设想阶段）：
  思路：参考 GitHub 开源项目搭建金融 Multi-Agent
  → 决策：不参考，独立从零实现

v1（2026-05-15，阶段一至六）：
  Agent：4 个分析 Agent + Summary（LLM 直接汇总）
  数据：仅 Tushare Pro
  UI：Gradio（调试用）
  评测：评判 Agent 代码完成
  ✅ 首轮全量测试 PASS（000001.SZ 平安银行）

v2（2026-05-17，阶段七至八）：
  Agent：Summary 升级为 ReAct Agent（3工具），7条铁律，两段式输出
  数据：Tushare → akshare 备选
  UI：React 19 + FastAPI + SSE（Gradio 降级为调试入口）
  前后端：打通 SSE 流式推送
  认证：邮箱验证码登录 + 游客模式
  ✅ 前后端整合完成

v3（2026-05-18，阶段九至十）：
  格式：服务端+客户端双重清洗，去 Markdown 符号
  数据：akshare 翻为主力，Tushare 降为备选
  搜索：A股注册表 5517 只，双层校验
  部署：GitHub 开源 → Railway → Render
  ✅ 上线 Render

v4（2026-05-19，阶段十一）：
  数据：新增 yfinance（海外兜底）
  部署：Render → Sealos 国内云
  CI/CD：GitHub Actions + 阿里云 ACR
  数据源翻回 akshare 主力
  ✅ Sealos 国内部署
```

### 为什么要迭代？

1. **UI 框架从 Gradio 换成 React + FastAPI**：Gradio 适合快速 demo，但面试需要展示一个"像产品的东西"，而且 Gradio 的 SSE 支持和前端定制能力都不够。评估后 Gradio 降级为开发调试入口。

2. **Summary 从纯 LLM 汇总升级为 ReAct Agent**：最初 Summary 就是把 4 个 Agent 的文本拼起来让 LLM 总结。考虑到简历 bullet 需要有"MCP 工具链"的深度，给 Summary 加了 3 个专属工具（交叉验证/数据一致性/加权评分），变成一个真正有推理能力的节点。

3. **数据源从单源升级为三级降级**：Tushare 单源 → Tushare+akshare 双源 → akshare+Tushare+yfinance 三级。每次升级都是被实际情况逼出来的（Tushare 限流、美国 IP 被墙）。

4. **输出格式从自由文本升级为铁律约束+两段式**：最初 Agent 输出什么格式取决于 prompt 怎么写，导致格式不稳定。升级为统一的铁律+模板+JSON Schema+双重清洗后，输出质量才稳定下来。

---

# 2. UI/UX 设计

## 2.1 前端设计流程回顾

### 实际经历的设计路径

```
阶段一：想法模糊
  → 不知道要做成什么样，只有功能列表在脑子里

阶段二：用 Image2（AI 图片生成）出原型图
  → 把功能需求转成视觉草图
  → 帮助自己在脑子里形成"这个东西长什么样"的画面
  → 原型图质量取决于 prompt 描述能力，当时还不太会写视觉 prompt

阶段三：多个模型按原型图出代码
  → 把原型图丢给不同模型（Gemini/Claude/GPT）生成前端代码
  → Google AI Studio（Gemini）生成的脚本效果最好
  → 虽然迭代时不听话（改了A它改B），但能跑通基本 page 构想
  → 其他模型生成的代码也能跑，但在设计感上差距明显

阶段四：交给 Claude Code 做前后端整合
  → localhost 验证基本功能
  → 不断调整 UI/UX（字体/颜色/间距/布局）
  → Claude Code 用 UI/UX Pro Max skill 后设计质量有明显提升
  → 但和理想效果仍有差距

阶段五：主动提升审美
  → 浏览优秀设计和配色的网站（Dribbble、Behance 等）
  → 看别人用 AI 设计的作品（Twitter/X 上的 AI design 分享）
  → 逐渐形成了对"专业研报风格"的审美判断
  → 定下"反 AI 元素"设计原则
```

### Claude Code 设计能力的边界

**Claude Code 做不好的**：
- 直接从图片"参考"的设计，结果往往偏离预期——Claude 无法真正"看"图片，只能从文字描述中推断视觉风格
- 初始默认设计偏"AI 产品模板"（紫色渐变、机器人 icon、打字机效果），缺乏独特性
- 字体选择、颜色搭配的"品味"不如专业设计师

**用了 UI/UX Pro Max skill 后改善的**：
- 设计更系统化，有设计令牌（CSS 变量）、字体体系
- 开始能输出专业研报风格（中金/中信/摩根士丹利参考）
- 配色从紫色渐变转向暖调专业色（floral-white/dust-grey/charcoal-brown/carbon-black/spicy-paprika）

**根本限制**：
- Claude 没有视觉审美能力，它只能按照指令执行
- 最终的"品味"来自用户的审美判断和方向指导
- AI 是执行工具，设计方向和审美标准必须由人来定

### "反 AI 元素"原则的由来

浏览了大量 AI 产品后，发现几乎所有 AI 产品都在用同一套视觉语言：紫色渐变背景、机器人/Sparkles 图标、打字机动画、✨ emoji。这种同质化让产品看起来像"套模板的又一个 AI demo"而非专业金融工具。

于是定了 5 条反 AI 原则：
1. 禁止紫色/蓝色渐变
2. 禁止机器人/Sparkles 图标
3. 禁止✨/🤖 emoji
4. 禁止"AI-Powered"、"智能"、"赋能"等标签
5. 禁止打字机效果、脉冲呼吸动画

参考方向转为专业研报（中金/中信/摩根士丹利）：克制、权威、信息密度高。

## 2.2 前端技术实现

| 模块 | 技术 | 关键点 |
|------|------|--------|
| 落地页 | Three.js K 线图 | 确定性种子 seededRandom(42)，160 根固定 K 线 + 2 MA，动态发光脉冲 |
| 登录 | 两步验证码流程 | Resend API 邮件 + 降级终端打印 + 游客模式跳过 |
| Dashboard | SSE 流式绑定 | 4 Agent 卡片实时状态更新，Agent Modal + FormattedMarkdown 渲染 |
| 报告页 | 研报风格排版 | Noto Serif SC 标题 + 仿宋二级标题 + HTML table + 全宽布局 |
| 配置 | localStorage 持久化 | LLM-only 配置，数据源免配 |
| 图表 | SVG 零依赖 | RadarChart + BarChart，无第三方库 |

## 2.3 字体与排版体系

```
品牌标题（Nice Invest）  → Playfair Display 700 italic
副标题                   → DM Sans 300
正文                     → Inter / PingFang SC / Microsoft YaHei
研报标题                 → Noto Serif SC
二级标题/表格说明        → FangSong / 仿宋 / STFangsong bold
技术数据                 → Space Grotesk
```

这个字体体系参考了专业金融研报的排版惯例：serif（衬线体）传递权威感，sans-serif（无衬线体）用于正文保证可读性，仿宋用于二级标题是中文研报的传统做法。

## 2.4 怎样让下一次开发不卡在前端设计阶段

### 这次卡在哪了

1. **起点太模糊**：一开始只有功能列表，没有视觉参考系。不知道"好"长什么样子，就无法评判 AI 生成的结果是"好"还是"不好"。

2. **把 AI 当设计师用**：直接让 Claude "参考这张图做个好看的页面"。但 Claude 无法真正理解图片的视觉语言——它只能从文字描述推断。结果往往是"看起来像但感觉不对"。

3. **改起来太费劲**：前端代码一旦生成，后续在代码层面微调颜色/间距/字体非常低效。一个字体改动可能涉及多处 CSS。

4. **审美标准是后来建立的**：浏览了 Dribbble、Behance、Twitter 上别人用 AI 做的设计之后，才知道什么方向是对的。这个"找参考"的步骤应该在动手写代码之前做。

### 可重复的流程（下次这样做）

**第一步：找参考（写代码前，花 1-2 小时）**

- 至少找 5 个同类型产品的截图作为 mood board
- Dribbble 搜关键词（如 "financial dashboard", "investment report"）
- Twitter/X 上搜 "AI design" "v0 design" 看别人用 AI 工具做的作品
- 不是找"最漂亮的"，而是找"最适合这个产品气质的"

**第二步：定设计系统（写代码前，花 30 分钟）**

- 配色方案：3-5 个颜色 + 用途定义
- 字体体系：标题/正文/数据 各用什么
- 组件规范：按钮/卡片/表格长什么样
- 把这些写成设计文档（text，不是代码），方便后续 AI 理解和执行

**第三步：用原型工具而非直接写代码**

- Figma / Excalidraw 做低保真线框图
- 或者用 AI 图片生成工具（Midjourney/DALL-E）出高保真视觉稿
- 线框图解决了"东西怎么布局"，视觉稿解决了"东西长什么样"
- 这一步不需要 pixel-perfect，目标是让脑子里有清晰的画面

**第四步：拿参考图 + 设计文档让 AI 写代码**

- 输入：5 张 mood board 截图 + 设计系统文档 + 线框图
- 要求：先输出组件结构规划，确认后再写代码
- 这样 AI 有明确的视觉目标，不会乱发挥

**第五步：用浏览器 DevTools 微调，而非让 AI 反复改代码**

- 颜色、间距、字号这些在 DevTools 里直接改，实时看到效果
- 调满意了再让 AI 把改动合入代码
- 效率比"让 AI 改 → 刷新看效果 → 不满意再让 AI 改"高很多

### 核心原则

> **AI 是执行工具，设计方向和审美标准必须由人来定。** 在你自己都不知道"好看"长什么样之前，AI 生成的东西大概率不会让你满意。先建立视觉参考系，再让 AI 执行。

---

# 3. 网站部署

## 3.1 部署历程（3 次迁移）

```
Render（美国）→ Railway（尝试，构建失败）→ Render（成功）→ Sealos 国内云（最终）
```

| 平台 | 结果 | 失败原因 |
|------|------|---------|
| Railway | ❌ 构建失败 | nixpacks 基础设施问题 "secret https not found" |
| Render | ✅ 部署成功 | 但数据源全部失效（B16：美国 IP 被墙） |
| Sealos | ✅ 最终部署 | 国内云，akshare 正常工作 |

## 3.2 全量 Bug 清单与根因分析

### 部署相关 Bug（B9-B17）

| Bug | 现象 | 根因 | 解决方案 |
|-----|------|------|---------|
| **B9** | Railway 启动崩溃 `ModuleNotFoundError: config` | `config.py` 被 `.gitignore`，线上无此文件；但 5 个模块有 `import config` | 全部改为 `try/except ImportError` + `os.environ.get()` 兜底 |
| **B10** | Railway 重启循环 | Railway 自动检测 `main.py` 为入口；Gradio 6.0 `Blocks()` 不接受 `css` 参数 | ① css 移至 `launch()` ② 新增 Procfile 指定 `python server.py` |
| **B11** | Landing 页面黑屏 | `web/dist/landing.html` 被清理后未重新构建 | 清理 dist 后完整 rebuild |
| **B12** | 体验 Key 不生效（第一轮） | `_apply_llm_config` 只设了 `OPENAI_API_KEY`，忘了设 `DEEPSEEK_BASE_URL`；`get_llm()` deepseek 分支读空 base_url | **关键教训**：设置环境变量时，必须覆盖所有模型族的对应变量（OPENAI / DEEPSEEK / QWEN 三组），因为 `get_llm()` 中不同分支读不同的 env var |
| **B13** | 标签页标题显示 "My Google AI Studio App" | `index.html` title 是 AI Studio 默认值，未修改 | 改为 "Nice Invest" |
| **B14** | 体验 Key 不生效（第二轮） | 之前一次提交 `e2c60f8` 简化了 `_apply_llm_config`，删除了 config.py 兜底逻辑，只剩 `os.environ.get("DEMO_API_KEY")`；本地开发时环境变量通常不设，所以体验 Key 读不到 | 恢复三级兜底：os.environ → config.py → 默认值 |
| **B15** | Railway 构建失败 "secret https not found" | Railway nixpacks 基础设施问题，与代码无关 | 添加 Dockerfile，切 Render Docker 部署 |
| **B16** | Render 部署后数据源全部失效，前端评分全 0 | Render 美国 IP → akshare HTTP 调用东方财富/同花顺/财联社超时（被墙）；Tushare Token 未配置 → 所有 Agent 无数据可用 → 回退 LLM 知识 → 无结构化 JSON → 前端显示"数据源不可用" | ① 新增 yfinance 数据源（Yahoo Finance 美国可秒拉）② GitHub Actions + 阿里云 ACR 国内镜像仓库 ③ Sealos 国内云部署 |
| **B17** | Sealos 部署后 API Key 仍报 401 | `_apply_llm_config` 只读 `DEMO_API_KEY` 环境变量，且 else 分支无论 demo_key 是否为空都执行 `os.environ["OPENAI_API_KEY"] = demo_key`，导致用户设了 `OPENAI_API_KEY` 反而被空字符串覆盖 | ① Key 读取扩展为五级：`DEMO_API_KEY` → `OPENAI_API_KEY` → `DEEPSEEK_API_KEY` → config.py → 硬编码兜底 ② 只在找到非空 Key 时才 `os.environ[key] = demo_key` |

### 用户侧 Bug（U1）

| Bug | 现象 | 根因 | 教训 |
|-----|------|------|------|
| **U1** | Sealos 环境变量配置后体验 Key 不生效 | 把 `DEMO_BASE_URL` 也设成了 `DEMO_API_KEY`，导致 `DEMO_API_KEY` 的值被 URL 覆盖 | 部署平台配环境变量时，逐条确认变量名拼写。Name 和 Value 一一对应，不要两个变量用同一个 Name |

### Agent 层 Bug（B1-B8）

| Bug | 现象 | 根因 | 解决方案 |
|-----|------|------|---------|
| **B1** | 所有 Agent 初始化崩溃 | `template.py` 中 JSON Schema 的花括号被 `ChatPromptTemplate.from_messages()` 误解析为 f-string 模板变量 | 所有非模板变量的花括号转义为 `{{` `}}` |
| **B2** | 邮箱登录卡住用户 | Resend 邮件无法送达（域名未验证），降级提示用户不理解 | 新增"跳过登录，直接体验"游客模式 |
| **B3** | Vite 端口漂移导致 CORS 拦截 | Vite 默认端口被占用时自动 +1，不在 CORS 白名单中 | CORS 白名单新增 3001/3002 |
| **B4** | 历史记录查询 500 错误 | `return {"found": False, "record": null}` — `null` 在 Python 中未定义 | `null` → `None` |
| **B5** | Agent 输出混入思考过程 | DeepSeek 倾向输出推理链（"好的"、"现在我来..."） | 铁律强化 + 服务端/客户端双重清洗 |
| **B6** | Tushare 限流导致分析失败 | 所有 Agent 优先调 Tushare，限流后才回退 akshare | 4 个 Agent 全部改为 akshare 优先 |
| **B7** | 报告页裸 Markdown 无层次 | 前端仅 whitespace-pre-wrap | FormattedMarkdown/ReportMarkdown 组件 |
| **B8** | 股票代码替代名称显示 | `stock_name || stock_code` 逻辑，name 为空时直接显示代码 | stockDisplayName() 查本地表兜底 |

## 3.3 环境变量配置的核心教训

### 问题本质

`_apply_llm_config()` 这个函数是整个部署过程中 bug 最多的单点。核心矛盾在于：

**LLM 配置的上下文传递路径很长**：
```
用户前端配置页（可选）
  → POST /api/analyze 请求体 llm_config
  → server.py _apply_llm_config(llm_config)
  → os.environ 动态设置（覆盖全局进程环境变量）
  → graph.py get_llm() 按 model 分支读取不同的环境变量
  → LangChain ChatOpenAI 实例创建
```

这个链条上每个环节都可能出问题，而且排查困难——错误现象是"分析失败"或"401"，但根因可能是链路中任意一个节点的错误。

### 踩过的三个关键坑

**坑 1：环境变量作用域不一致**

`get_llm()` 中，不同模型分支读不同的环境变量：
- gpt-4o → `OPENAI_API_KEY` + `OPENAI_BASE_URL`
- deepseek → `DEEPSEEK_API_KEY` + `DEEPSEEK_BASE_URL`
- qwen → `QWEN_API_KEY` + `QWEN_BASE_URL`

所以 `_apply_llm_config` 必须同时设置三组环境变量。只设 OPENAI 那组会导致 deepseek 模型读空值（B12）。

**坑 2：空值覆盖已配置的变量**

这行代码：
```python
os.environ["OPENAI_API_KEY"] = demo_key  # 如果 demo_key 是空字符串，会把已有值覆盖掉
```

修复后必须加判断：
```python
if demo_key:
    os.environ["OPENAI_API_KEY"] = demo_key
```

这是 B14 和 B17 的共同根因。

**坑 3：环境变量命名不统一**

不同平台/不同人的习惯下，同一个 API Key 可能叫：
- `DEMO_API_KEY`（项目自定义）
- `OPENAI_API_KEY`（OpenAI 兼容的业界惯例）
- `DEEPSEEK_API_KEY`（模型专用的命名）

修复后的五级兜底链：
```
DEMO_API_KEY → OPENAI_API_KEY → DEEPSEEK_API_KEY → config.py → 硬编码
```

### 最终体验 Key 逻辑

```
用户点击分析
  ├─ 用户在配置页填了 Key → 用用户的 Key
  └─ 用户没填 Key → 后端自动注入：
       DEMO_API_KEY（环境变量，推荐）
       → OPENAI_API_KEY（兼容命名）
       → DEEPSEEK_API_KEY（兼容命名）
       → config.DEMO_API_KEY（本地开发）
       → 硬编码兜底 Key（终极保底，server.py 内置）
```
无次数限制，无需用户做任何配置。

## 3.4 可复现的部署流程

### 前提条件

- GitHub 仓库已创建
- 阿里云 ACR（容器镜像服务个人版，免费）已开通
- Sealos 账号已注册

### Step 1：准备 Dockerfile（多阶段构建）

项目根目录已有的 `Dockerfile`：
- Stage 1：Node 20 构建前端（`npm ci && npm run build`）
- Stage 2：Python 3.11 运行后端（`pip install -r requirements.txt`）+ 复制前端 dist
- 入口：`python server.py`（端口从 `$PORT` 环境变量读取）

### Step 2：配置 GitHub Actions Secrets

在 GitHub 仓库 Settings → Secrets and variables → Actions 中设置：

| Secret | 值 |
|--------|-----|
| `ACR_REGISTRY` | `registry.cn-hangzhou.aliyuncs.com/<namespace>/<repo>:latest` |
| `ACR_USERNAME` | 阿里云 ACR 用户名（个人版为阿里云账号 UID） |
| `ACR_PASSWORD` | 阿里云 ACR 访问凭证（在 ACR 控制台"访问凭证"获取） |

### Step 3：推送代码触发 CI/CD

```bash
git push origin master
```

GitHub Actions 自动执行：
1. 拉取代码
2. Docker 多阶段构建（前端 + 后端）
3. 推送镜像到阿里云 ACR
4. 标签：`latest` + `commit SHA`

### Step 4：Sealos 部署

1. Sealos 控制台 → 应用管理 → 新建应用
2. 镜像地址：填入 ACR 镜像地址
3. 端口：8000（`$PORT` 默认值）
4. 环境变量：

| 变量名 | 值 |
|--------|-----|
| `DEMO_API_KEY` | 体验用 API Key（如 DeepSeek Key） |
| `DEMO_BASE_URL` | API 端点（如 `https://api.deepseek.com/v1`） |
| `PORT` | `8000` |

5. 点击部署，等待 Pod 启动
6. 访问 Sealos 分配的域名

### Step 5：验证

```bash
# 健康检查
curl https://<your-domain>.sealos.run/api/health
# 应返回 {"status": "ok", "version": "0.2.1"}

# 验证日志（Sealos 控制台 → Pod → 日志）
# 应看到: [CONFIG] 环境变量诊断: DEMO_API_KEY=SET:sk-xxx***, ...
```

### 常见问题排查

| 症状 | 检查项 |
|------|--------|
| 页面打不开 | Sealos Pod 是否 Running？端口是否正确？ |
| 分析失败 401 | 日志中 `[CONFIG] 环境变量诊断` 看 DEMO_API_KEY 是否 SET |
| 数据全是 0 | 是否在非中国 IP 访问？（海外 IP 无法访问东方财富/同花顺等国内源）|
| 构建失败 | GitHub Actions 日志 → 检查 ACR secrets 是否正确 |

---

## 附录：项目文件结构速览

```
LangGraph-financial-agent/
├── src/
│   ├── graph.py              ← 编排核心（Router → 并行 Agent → Summary）
│   ├── state.py              ← 状态定义（agent_results 使用 add reducer）
│   ├── agents/
│   │   ├── template.py       ← 7 条铁律 + 5 种 JSON Schema
│   │   ├── analyst.py        ← 基本面 Agent（5 tools）
│   │   ├── technical.py      ← 技术面 Agent（3 tools）
│   │   ├── valuation.py      ← 估值 Agent（6 tools）
│   │   ├── news.py           ← 新闻 Agent（5 tools）
│   │   └── summary.py        ← Summary Agent（3 tools）
│   ├── mcp_tools/
│   │   ├── news_api.py       ← akshare 封装（主力，15s 超时）
│   │   ├── tushare_api.py    ← Tushare Pro 封装（备选1）
│   │   ├── yahoo_api.py      ← yfinance 封装（海外兜底）
│   │   └── calculator.py     ← 杜邦/PEG/CAGR 计算
│   ├── auth.py               ← 邮箱验证码登录
│   ├── logger.py             ← 日志系统
│   └── stock_registry.py     ← A 股注册表（5517 只）
├── server.py                 ← FastAPI 入口（REST + SSE + 静态文件）
├── main.py                   ← Gradio 调试入口（已降级）
├── web/                      ← React 前端（3 页 SPA）
├── tests/                    ← 测试脚本与报告
├── .github/workflows/        ← CI/CD（自动构建 + 推送 ACR）
├── Dockerfile                ← 多阶段构建
├── render.yaml               ← Render 配置（已停用）
└── PROGRESS.md               ← 进度看板
```

---

> **下一步**：评判 Agent 的真实评测工作（4 维度打分 + 多模型对比跑通 + 评测报告产出）是项目第二阶段的核心任务。
